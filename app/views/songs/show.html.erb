<h1><%= @song.artist.name %> - <%= @song.title%></h1>

<!-- Add code here to link to the post's category. -->
<h3>Artist: <%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %></h3>
